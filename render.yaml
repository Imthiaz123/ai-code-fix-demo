services:
  - type: web
    name: ai-code-fix-demo
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
    autoDeploy: true
    envVars:
      - key: OPENAI_API_KEY
        sync: false
